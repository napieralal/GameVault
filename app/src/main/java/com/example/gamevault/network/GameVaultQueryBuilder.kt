package com.example.gamevault.network

object GameVaultQueryBuilder {
    /*fun searchGamesByName(name: String): String {
        return "search \"$name\"; fields name,cover.url,genres.name,first_release_date,rating; limit 20;"
    }

    fun getGameById(id: Long): String {
        return "fields name,cover.url,summary,genres.name,platforms.name,rating; where id = $id;"
    }

    fun getTopRated(): String {
        return "fields name, rating, cover.url; sort rating desc; limit 10;"
    }*/
}